<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:orientation="vertical"
    android:layout_width="fill_parent"
    android:layout_height="fill_parent"
    >
<include
    android:id="@+id/title_bar"
    layout="@layout/top_nav_layout">
</include>
<org.codeanywhere.openread.view.PullToRefreshView xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/main_pull_refresh_view"
    android:background="#ffffff"
    android:layout_width="fill_parent"
    android:layout_height="fill_parent" 
    android:orientation="vertical">
	<!-- 这里放置listview,gridview或者scrollview的布局 ,PullToRefreshView
	要设置android:orientation="vertical"属性
	否则,显示不正确-->
    <GridView
        android:id="@+id/gridview"
        android:numColumns="2"
        android:verticalSpacing="10dip"
        android:layout_width="fill_parent"
        android:layout_height="fill_parent" />

</org.codeanywhere.openread.view.PullToRefreshView>

</LinearLayout>